# Connection URL is NOT set here (a '%' in the password would clash with
# configparser interpolation). migrations/env.py reads it from config.Settings.
[alembic]
script_location = migrations
prepend_sys_path = .
path_separator = os
